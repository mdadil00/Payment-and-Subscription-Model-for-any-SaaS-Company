# ONE
http.get./all/course/{id}:
  summary: Fetch Course
  description: Fetch Course from database
  fn: com.biz.all.course.one
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            type: object

