# SEARCH
http.post./all/user/search:
  summary: Fetch multiple User
  description: Fetch multiple User from database
  fn: com.biz.all.user.search
  authn: false   # This enables/disables auth
  body:
    content:
      application/json:
        schema:
          type: object
  responses:
    '200':
      content:
        application/json:
          schema:
            type: array

